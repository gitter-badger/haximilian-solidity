"use strict";
module.exports = {
  networks: {
    development: {
      host: "127.0.0.1",
      port: 8545, // eslint-disable-line no-magic-numbers
      network_id: "*" // eslint-disable-line camelcase
    }
  }
};
